#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main()
{
    int n, k;
    cin >> n >> k;
    // if(k>=n)k = k - n;
    // deque<int> nos;
    // for (int i = 1; i <= n; i++)
    //     nos.push_back(i);
    // for (int i = 0; i < n; i++)
    // {
    //     int it = 0;
    //     while (it < k)
    //     {
    //         int x = nos.front();
    //         nos.pop_front();
    //         nos.push_back(x);
    //         it++;
    //     }
    //     cout << nos.front() << " ";
    //     nos.pop_front();
    // }
    vector<int> v(n);
    map<int, int> mp;
    for (int i = 1; i < n+1s; i++)
    {
        cin >> i;
        mp[i]++;
    }
    for (int i = 0; i)
        return 0;
}